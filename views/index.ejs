<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
    <div class="container">
        <header>
            <div class="compass-icon"><img src="/img/compass.png" alt="" /></div>
        </header>
        <div class="weather-title">
            <h1>Weather Informant</h1>
            <p>View the Current Weather of Your City</p>
        </div>
        <form action="/" method="POST">
            <input type="text" name="city" class="input" placeholder="Enter a City" />
        </form>

        <div class="result">
            <% if(temp !== null)  {%>
                <h1 class="temp">
                     <%= Math.round(((temp-273.15) + Number.EPSILON) * 100) / 100 %>&#176;C
                </h1>
                <% } %>

                    <div class="icon-des">
                        <% if(icon !== null) { %>
                            <img class="icon" src="http://openweathermap.org/img/w/<%= icon %>.png" alt="" />
                            <% } %>
                                <% if(des !== null) {%>
                                    <h3>
                                        <%= des %>
                                    </h3>
                                    <% } %>
                    </div>

                    <% if(city !== null)  {%>
                        <p class="city">
                            <%= city %>
                        </p>
                        <% } %>
        </div>
    </div>
</body>

</html>
